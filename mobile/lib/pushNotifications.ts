// Push notifications disabled — expo-notifications plugin not configured yet
// Re-enable after configuring FCM credentials in EAS

export async function registerPushToken(): Promise<void> {
  // no-op until notifications are configured
}

export async function unregisterPushToken(): Promise<void> {
  // no-op until notifications are configured
}
